#include "filesys.h"

#define NAMELEN 30
#define INLEN 2
#define TOTLEN (NAMELEN + INLEN)

int showfile_myfs (char *filename)
{
	in fnode =  & myfs -> inlist[myfs->pwd];//
	int w_size = 0;
	
	for (int i = 0; i < 9; ++i)
	{
		/* code */
		if(dirbl[i] == cloc)
		{

			break;
		}

		else
		{
			
		}


	}

}